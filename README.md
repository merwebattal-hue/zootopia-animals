# Zootopia Animals

This project generates an HTML page using Python, JSON data, and an HTML template.

## How it works
- animals_data.json → data source
- animals_template.html → HTML template
- animals_web_generator.py → generator script
- animals.html → generated output
